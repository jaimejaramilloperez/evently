namespace Evently.Modules.Ticketing.Presentation;

internal static class Tags
{
    internal const string Carts = nameof(Carts);
    internal const string Orders = nameof(Orders);
    internal const string Tickets = nameof(Tickets);
}
